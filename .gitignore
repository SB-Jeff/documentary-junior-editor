# Secrets — never commit
.env
.env.local
*.env

# Claude Code per-session state (machine-local); but DO track the shared
# project settings (the SessionStart re-entry hook) so it travels across clones.
.claude/*
!.claude/settings.json

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store
Icon?
