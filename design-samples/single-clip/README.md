# Single-Clip Design Samples — Nanos 2026 Boston

These are reference FCPXMLs for the single-clip interview footage scenario.
Source: Nanos 2026 Boston / Ben (event promo).

- `Ben_captioned_interview.fcpxml` — full single-cam captioned interview. Single `<asset-clip>` on the spine with captions attached directly. Sequence tagged `single cam - event promo`.
- `Sample_narrative.fcpxml` — sample narrative timeline using single-clip sources. 6 `<asset-clip>` elements on the spine, each referencing its own `<asset>` resource.

Used by `SKILL-fcpxml-params.md` and `SKILL-fcpxml.md` to document the single-clip code path (Lesson 10, v[next]).
